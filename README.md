# Invoice Management System

## Requirements
- PHP 8+
- Laravel 9
- MySQL
- Node.js & npm
- Optional: Vue.js frontend

## Setup
1. Copy `.env.example` → `.env` and set your DB credentials
2. Install dependencies:
# invoice-system
Laravel 9, mySQL, built in auth (breez), vuejs frontend, pagination, invoice belongs to customer 

composer install
npm install
npm run dev
3. Run migrations and seeders:

php artisan migrate --seed


